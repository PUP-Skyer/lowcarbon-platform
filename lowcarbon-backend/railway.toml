[build]
builder = "nixpacks"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar target/lowcarbon-backend-1.0.0.jar"
healthcheckPath = "/api/data/kpi"
healthcheckTimeout = 60
restartPolicyType = "on_failure"