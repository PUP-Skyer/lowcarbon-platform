package com.lowcarbon.repository;

import com.lowcarbon.entity.CreditSource;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CreditSourceRepository extends JpaRepository<CreditSource, Long> {
}
